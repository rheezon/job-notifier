spring.application.name=job-notifier

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/job_notifier_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=yourSecretKeyHerePleaseChangeThisToALongRandomSecureKeyForProduction
jwt.expiration=86400000

# Scheduler Configuration
scheduler.fixed-rate=3600000
scheduler.max-runs=10
scheduler.enabled=true

# AI Configuration
openai.api.key=your-openai-api-key-here
openai.model=gpt-4
ai.relevance.threshold=0.7
ai.prompt.template=Analyze the following job posting and user preferences. Score the relevance from 0 to 1.\n\nJob: {job}\n\nUser Preferences:\nCity: {city}\nSalary: {salary}\nCompanies: {companies}\nExperience: {experience}\nNotice Period: {noticePeriod}\nCollege: {college}\n\nProvide only a JSON response with keys: 'score' (number 0-1) and 'reason' (string).

# Cloudinary Configuration
cloudinary.cloud-name=your-cloud-name
cloudinary.api-key=your-api-key
cloudinary.api-secret=your-api-secret

# LaTeX Configuration
latex.compiler.url=https://latexonline.cc/compile
latex.timeout=30000

# Server Configuration
server.port=8080

# Logging
logging.level.com.jobnotifer=DEBUG
logging.level.org.springframework.security=DEBUG
